<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="by.belhard.activity"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <provider 
            android:name="by.belhard.db.TransportContactProvider"
            android:authorities="by.belhard.db.transportcontactprovider"/>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity 
            android:name=".TimetableActivity"
            android:label="@string/timetable">
        </activity>
        <activity android:name=".BusActivity"></activity>
        <activity android:name=".TrolleybusActivity"></activity>
        <activity android:name=".TramActivity"></activity>
        <activity android:name=".SearchActivity"></activity>
        <activity android:name=".PreferencesActivity"></activity>
    </application>

</manifest>